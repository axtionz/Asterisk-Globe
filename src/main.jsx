import React from 'react'
import ReactDOM from 'react-dom/client'
import OriginalApp from './OriginalApp.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <OriginalApp />
  </React.StrictMode>,
) 